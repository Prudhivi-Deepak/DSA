export interface TaskType {
  id: string;
  userId: string;
  title: string;
  time: string;
  description: string;
}

export interface newTaskType {
  title: string;
  description: string;
  time: string;
}