Event loop ==> keeps on running as long as event listeners are present
process.exit() ==> to exit the event loop but no one wants to do that because it's no longer accessible to people

response
res.setHeader('Content-Type', 'text/html'); ==> to set the header of the response
 - it says the type of content that is being sent to the client is html

res.write() ==> to write data to the response body
    - it can be called multiple times to send multiple chunks of data
    - error can occur if res.write() is called after res.end()
res.end() ==> to end the response
    - it tells the server that all of the response headers and body have been sent
------------------------------------------------------------------------------------------

streams & buffers
consider a big file being uploaded to a server
- if the server tries to read the entire file into memory at once, it can lead to high memory usage and potential crashes
- instead, the server can read the file in smaller chunks using streams
- streams allow data to be processed piece by piece, reducing memory consumption and improving performance
buffer is like a bus stop
 - hold multiple chunks of data temporarily before they are processed/released or sent to their destination


req.on() ==> on allows us to listen to different events like data event /(close, end, error, readable, data events)
    - data event ==> triggered when a new chunk of data is available to read from the stream
    - end event ==> triggered when there is no more data to read from the stream
    - error event ==> triggered when an error occurs while reading from the stream
    - readable event ==> triggered when the stream is ready to be read
    - close event ==> triggered when the stream is closed
req.on('data', (chunk) => {
    // arrow function to process each chunk of data as it arrives
});

when ever stream is end we can use end event
req.on('end', () => {
    // we can conver all the chunks stored in array body to buffer using Buffer.concat() 
    // then buffer to string using toString() 
    
    // code inside this arrow function in this event listener will do it eventually,
    // so will get error if we send response here and also outside the event listener
    
    res.end() 
    // this shows error 
    // or this works fine if we don't have an return outside the listeners because function ends 
    // with no return and eventually this event comes to end and returns this res.end()
    // or just return the event listener function itself to make it work what ever (return req.on('end', () => {...}))

    writeFileSync('user_data.txt', parsedData); 
    // This blocks the code execution until the file is written completely.
    // so, it's better to use asynchronous version writeFile() with a callback (function that executes when the file is written) or promise
    // if you want to return the response after writing the file then in the call back function return the res.end()


});
res.end() // this works
return res.end() works same as res.end() here
Node Js has some registry having all listerners and call that function after parsing
------------------------------------------------------------------------------------------
 
vanilla Nodejs
Adonis JS
koa
sails JS
express

------------------------------------------------------------------------------------------
Express Js

It's all about middleware functions
A middleware function is a function that has access to the request object (req), 
                                                    the response object (res), and 
                                                    the next middleware function 
                                    in the application's request-response cycle.
multiple middleware functions can be chained together to handle a request instead of a huge function doing everything

const express = require('express'); //exports a function
const app = express(); //app is a valid request handler
createseerver(app);//pass app to server

app.use() ==> allows us to add new middleware functions to the application
    - it takes a middleware function as an argument
    - the middleware function can perform any task, such as logging, authentication, parsing request bodies, etc.
    - after performing its task, the middleware function can either send a response to the client
         or call the next() function to pass control to the next middleware function in the chain
    -works fine with bothe GET and POST request

//can't rename req, res, next
app.use((req, res, next) => {
    console.log('Middleware 1: Logging request details');
    console.log(`Request Method: ${req.method}, Request URL: ${req.url}`);
    next(); // Pass control to the next middleware function
});

app.use((req, res, next) => {
    console.log('Middleware 2: Authenticating user');
    res.send('Response from send'); // Utility function send response to the client by automatically setting headers and status codes
});

// ---------------------------------------------------------------
const server = http.createServer(app);//pass app to server
server.listen(3000)
app.listen(3000); //shortcut to create server and listen on port 3000 --> instead of above 2 lines
// ---------------------------------------------------------------

res.send("") // sends the response to the client by automatically setting headers and status codes
res.redirect('/'); //sets status code 302 and Location header
------------------------------------------------------------------------------------------

req.body ==> not able to parse rew automatically in express
    - need to use middleware to parse the body of incoming requests
    - use body-parser (sometimes comes pre-installed with express)

const bodyParser = require('body-parser');
app.use(bodyParser.urlencoded({ extended: false }));
    - this middleware will parse the body of incoming requests with urlencoded payloads
    - extended: false means that the values can be only strings or arrays
    - if true, it can parse nested objects as well
------------------------------------------------------------------------------------------

app.get() ==> to handle GET requests to a specific route
    - takes two arguments: the route path and a callback function

app.post() ==> to handle POST requests to a specific route
    - takes two arguments: the route path and a callback function
---------------------------------------------------------------------------------------------

views ==> to store the templates for rendering dynamic HTML content
    - by default, express looks for views in a folder named 'views' in the root directory of the project
    - we can change the default views directory using 
        app.set('views', 'path/to/views')

static content ==> to serve static files like CSS, JS, images, etc.
    - use express.static middleware to serve static files from a specific directory
    - for example, to serve static files from a folder named 'public':
        app.use(express.static(path.join(__dirname, 'public')));
    - now, any file in the 'public' folder can be accessed directly via its URL
        e.g., if there's a file 'public/css/styles.css', it can be accessed at '/css/styles.css'
        in user.html we can use 
            <link rel="stylesheet" href="/css/styles.css">

----------------------------------------------------------------------------------------------
templating engines ==> to generate dynamic HTML content
    - allows us to create HTML templates with placeholders for dynamic data
    - when rendering the template, the placeholders are replaced with actual data

Pug ==> popular templating engine for Node.js
    - uses indentation-based syntax to define HTML structure
    - allows us to embed JavaScript code within the template to generate dynamic content
To use Pug with Express:
1. Install Pug using npm:
   npm install pug
2. Set Pug as the view engine in your Express app:
   app.set('view engine', 'pug');
3. Create Pug templates in the 'views' directory
4. Use res.render() to render a Pug template and send it as a response:
   res.render('template-name', { data: 'value' });
    - 'template-name' is the name of the Pug template file (without the .pug extension)
    - the second argument is an object containing data to be passed to the template

if condition & for loop in template in pub
example:
    if users.length > 0
        each user in users
            h1 #{user.title}
    else
        p No users found.

handlebars ==> another popular templating engine for Node.js
    - uses double curly braces {{}} to denote placeholders for dynamic data
    - allows us to create reusable templates with partials and helpers
To use Handlebars with Express:
1. Install Handlebars using npm:
   npm install express-handlebars@3.0
2. Set Handlebars as the view engine in your Express app:
    const exphbs = require('express-handlebars');
    app.engine('handlebars', exphbs());
    app.set('view engine', 'handlebars');
3. Create Handlebars templates in the 'views' directory
4. Use res.render() to render a Handlebars template and send it as a response:
   res.render('template-name', { data: 'value' });
    - 'template-name' is the name of the Handlebars template file (without the .handlebars extension because we set engine to 'handlebars')
    - the second argument is an object containing data to be passed to the template

<!-- {{#if users.length > 0}} -- this will show error because {{}} -- accepts true or false values , does not evaluate logic here -- move logic to express node js -->

if condition & for loop in template in handlebars
example:
    {{#if hasUsers}}
        {{#each users}}
            <h1>{{this.title}}</h1>
        {{/each}}
    {{else}}
        <p>No users found.</p>
    {{/if}}

{{{ body }}} ==> to render unescaped HTML content in Handlebars
    - by default, Handlebars escapes HTML content to prevent XSS attacks
    - if we want to render raw HTML content, we can use triple curly braces {{{ }}} instead of double curly braces {{ }}
main-layout.hbs ==> to define a common layout for all templates in Handlebars
    - allows us to define a common structure for all pages in the application
    - we can define a header, footer, and other common elements in the main layout
    - individual templates can then be rendered within the main layout using {{{ body }}}


ejs ==> another popular templating engine for Node.js
    - uses <% %> to denote JavaScript code and <%= %> to denote placeholders for dynamic data
    - allows us to embed JavaScript code within the template to generate dynamic content
To use EJS with Express:
1. Install EJS using npm:
   npm install ejs
2. Set EJS as the view engine in your Express app:
   app.set('view engine', 'ejs');
3. Create EJS templates in the 'views' directory
4. Use res.render() to render an EJS template and send it as a response:
   res.render('template-name', { data: 'value' });
    - 'template-name' is the name of the EJS template file (without the .ejs extension)
    - the second argument is an object containing data to be passed to the template

if condition & for loop in template in handlebars
example:
    <% if (users.length > 0) { %>
        <% for (let user of users) { %>
            <h1><%= user.title %></h1>
        <% } %>
    <% } else { %>
        <p>No users found.</p>
    <% } %>

layouts in EJS
    - allows us to define a common structure for all pages in the application
    - we can define a header, footer, and other common elements in the layout
    - individual templates can then be rendered within the layout using <%- include('template-name') %>
------------------------------------------------------------------------------------------

https://expressjs.com/en/guide/routing.html
Routing & advanced models


Route Params & query Params
-------------------------------------------------------------------------------

router.get('/product/:productId, ... )  --> here productId is Route params

... ==> in controller function we access the Route param using 
        req.params.productId

-------------------------------------------------------------------------------------
<a href="/admin/edit-product/<%=productId%>?edit=true" class="btn"> Edit</a> ==> in html both route and query params at once
router.get('/edit-product/:productId', adminController.getEditProduct)

req.query.edit ==> query params access in controller
req.params.productId ==> route params access in controller

------------------------------------------------------------------------------------

==> in html post request with body (input as hidden)

<form action="/cart" method="POST"> 
    <input type="hidden" name="productId" value="<%=product ? product.id : ''%>" />
    <button class="btn" type="submit"> add to cart </button>
</form>

router.post('/cart, ...)

...==> in controller
req.body.productId

----------------------------------------------------------------------------------------------------------

sql --> tables like users, products, orders
Data schema --> definition of table (data types )
Data relations --> 1 to 1, 1 to manya, many to many
example : SELECT * FROM Users where age>20;

NoSQL --> databse(shop) 
tables --> collections 
            --> records/documents(list of records)
                --> different fields {}
no strong schema, no data relations, no/few connections 

Horizontal          vs      vertical scaling
add more servers            Improve server capacity Hardware 
merge data --> DB


SQL                                         no SQL
---------------------------------------------------------------------------------------------
Horizontal scaling is difficult             BOth vertical horizontal scaling is posssible
vectical scaling is possible

data is distributed across multiple         data is typicaly merged/ nested in collections
tables

has schema and relations                    schema-lessa & no relations

limitations to thousands of read/write      greater performance with mass read & write requests
queries/second
---------------------------------------------------------------------------------------------------

My SQL installation

my sql server + workbench

and

npm install --save mysql2

2 ways to connect to database
    1. connect to db --> execute query --> then close the db connection
        --> re-execute connection code for every query
        --> time in efficient
    2. better way to connect is connection pool
        const pool = mysql.createPool({
            --> pass some information about the database host
            host: 'localhost',
            user: 'root',
            database: 'node_project',
            password: 'root'
        }) 
        --> pool manages multiple connections --> in which it will give a connection when we ask
        --> once the query is done, connection is handled back to the pool

        module.expots = pool.promise() => handles async tasks/data  instead of callbacks
                                        because it qallows us to write code in more structed way
                                       => we use promise chains instead of callbacks
        => these promises has 2 functions
            -> then() and catch()

insert query 

INSERT INTO `node_project`.`products` (`title`, `price`, `description`, `imageUrl`) 
VALUES ('flash', '19', 'awsome series', 'https://tinyurl.com/realflash');

db.execute("INSERT INTO products (title, price, imageUrl, description) VALUES (?, ?, ?, ?)", 
    [this.title, this.price, this.imageUrl, this.description]
)
---------------------------------------------------------------------------------------------

sequalize --> is an object relational mapping library
    - it allows us to interact with a SQL database using JavaScript objects instead of writing raw SQL queries
    - it provides a higher-level abstraction over the database, making it easier to work with data

sequelize documentsation : https://sequelize.org/docs/v6/
Sequelize Official Docs: http://docs.sequelizejs.com/
npm install --save sequelize mysql2

creates a new Sequelize instance to connect to a MySQL database (connection pool)
const sequelize = new Sequelize(
    'node_project',
    'root',
    'root',
    {
        dialect: 'mysql',
        host: 'localhost'
    }
);

to create a sequelize table model

const Product = sequelize.define('product', {
    id: {
        type: Sequelize.INTEGER,
        autoIncrement: true,
        allowNull: false,
        primaryKey: true
    },
    title: Sequelize.STRING,
    price: {
        type: Sequelize.DOUBLE,
        allowNull: false
    },
    imageUrl: {
        type: Sequelize.STRING,
        allowNull: false
    },
    description: {
        type: Sequelize.TEXT,
        allowNull: false
    }
});


It looks up all the models defined and creates tables for them --> sync our model with db

Sequelize.sync().then(result => {
    console.log(result);
    app.listen(3000);
})
.catch(err=>console.log(err))

To create a JS object in for the model

Product.build() --> to create the JavaScript Object alone
Product.create() --> to create and save the JavaScript Object

One quick note:
With Sequelize v5, findById() (which we'll use in this course) was replaced by findByPk().
You use it in the same way, so you can simply replace all occurrences of findById() with findByPk()

Product.findAll() --> to find the records with some filters as well
    --> this returns list of records
    --> if used where filter like 
        --> Product.findAll({ where: {id : productId }})
            --> this returns single record within a list

Product.findById() / findById()  --> find the single product by id column
    --> this returns only single output

All these functions return a promise, so we can use then() and catch() on them

Product.findbyPk(prodId).then(product=>{
    product.title = "changed title here";
    product.save();  --> to save the record back in db
}).catch();


Product.destroy(product) --> to destroy any product, we can keep it inside Preoduct.findAll().then(product=>{Product.destroy(product)})

Relate the models before sync in app.js
we can relate them using the keywords like belongsTo(), hasMany()
Product.belongsTo(User, {constraints: true, onDelete: 'CASCADE'});
User.hasMany(Product);

Sequelize.sync({force: true}) --> make the sync forcefully to reflect the new relations if tables are already created

Promise.resolve(user) --> to return a promise instead of user
technically we don't need Promise if we use then block, it automatically wraps it under promise

Inserting user into the incoming request
middle ware for incoming requests that's it
app.use((req, res, next)=>{
    User.findByPk(1).then(user => {
        req.user = user; //we are storing the sequelize object
        next();
    }).catch(err=>console.log(err));
});

now in the controllers we can create product like this

req.user.createProduct() --> methods provided by sequelize as it knows the relations

cart / product / user / cartItem ---> relationship

User.hasOne(Cart);
Cart.belongsTo(User); //optional
Cart.belongsToMany(Product, {through: CartItem});
Product.belongsToMany(Cart, {through: CartItem});


req.user.cart ==> undefined we can't use it as a attribute here
but we can get cart by "req.user.getCart()" in controller

I can do something like
cart.getProducts({ where: { id: productId } }) --> here we get only 1 product in list
because i have many products for a cart--> getProducts() --> magic method which returns the products with/ without filter

product = products[0]; --> we can assign the list to a variable
product.cartItem.quantity; --> you know what we have this cartItem (in between table) will be accessible from each product
--> we can access this magic variable and their attributes

we can update the cart by incrementing the quantity and then adding it back to cart
fetchedCart.addProduct(product, {
    through: { quantity: newQuantity }
})

we can also delete/destroy the cartItem (in between table records) using destroy with magic variable
product.cartItem.destroy();

Order / User / Product ------> relationship

Order.belongsTo(User);
User.hasMany(Order);
Order.belongsToMany(Product, {through: OrderItem});

Magic method on stored cart to set cartItems(table) to null
fetchedCart.setProducts(null); --> worked, made cartItems null

we need to include the products as array in the getOrders method to retreive about products in orders
Order.belongsToMany(Product, {through: OrderItem});
req.user.getOrders({include: ['products']}).then().catch();

we can loop through all the products in the template using 
orders.forEach(order =>{
    order.products --> parameters 
    --> if we iterate over this products we get each product and
    --> each product has a orderItem parameter
})

-----------------------------------------------------------------------------

Mongo DB --> A database engine that runs NoSQL 
Mongo DB Driver in NodeJs
MongoDB Official Docs: https://docs.mongodb.com/manual/core/security-encryption-at-rest/https://docs.mongodb.com/manual/
SQL vs NoSQL: https://academind.com/learn/web-dev/sql-vs-nosql/

npm install --save mongodb

// fXlPOiyRbmFQtiQc

to connect to mongo DB use the database.js/ databse_mongo.js code

To insert into the mongo collection witha  JS Object
db.collection('products').insertOne({title: 'flash', ...}); --> return a promise
db.collection('products').find() --> to find all documents in teh collection
    --> returns a cursor/ handle that tells the nodeJs okay give met he next document
    we can convert all docs to JS array like this
    --> db.collection('products').find().toArray().then().catch() --> for couple of 100 docs or else use pagination

To find an element

db.collection('products').find({id : productId}).next().then().catch() --> to filter and return 
next() --> to get me the next document 

db.collection('products')
.find(
    {_id : new mongodb.ObjectId(productId)} --> compare object in mongo db by wraping a new Object for productId string
)
.next()
.then(product=>{
    console.log(product);
    return product;
}).catch() 

if we are sure that we only have one product then use findOne()

db.collection('users').find({_id: new mongo.ObjectId(userId)}) --> returns promise

Update the collection docs using

dbOps = db.collection('products').updateOne({_id: new mangodb.ObjectId(this._id)},{ $set: this});

we use a special syntax to update this
takes an object where we describet he changes we make to the existing docs
{$set: this}

To find the specific products with a list of productIds
we can use the below syntax -- productIds (list of ids)
db.collection('products').find({_id: {$in : productIds}}); --> returns a cursor with all matching products
.toArray() --> to convert to JS array and return promise
---------------------------------------------------------------------------
Mongoose - ORM --> Object Document Mapping library
mongoosejs.com
Mongoose Official Docs: https://mongoosejs.com/docs/

npm install --save mongoose

we define the optional schema which is not restricted to be only that way, but we defien the basic schema for our collections
and export the model with a name and schema we created
const schema = mongoose.Schema;
const productSchema = new schema({
    title: {
        type: String,
        required: true
    },
    price: {
        type: Number,
        required: true
    },
    description: {
        type: String,
        required: true
    },
    ImageUrl: {
        type: String,
        required: true
    }
    
});
module.exports = mongoose.model('product', productSchema);

In mongoose we can use
Product.find() --> which gives the promise, not the cursor
Product.find().cursor() -->  to get the cursor if we need
Product.find().cursor().next() --> to get next docs


Product.findById(productId).then(product=>{
    product.save() --> product is a mogoose object, all methods works here 
    --> save works as to update the existing product in this case
})

To delete a record we have -->findByIdAndDelete
findByIdAndRemove is replaced by findByIdAndDelete
Product.findByIdAndDelete(productId) --> works fine

To add userId and tell it to refer other collection for full information
userId: {
    type: schema.Types.ObjectId,
    ref: 'User' --> exact same name when you are creating model and exporting
}

To retreive the other colelctions info using reference id in the current collection
like using userId to retreive all usersInfo in the userId field in products 
Product.find().populate('userId') --> populate helps us to do the heavy lifting
.then(products=>{
})

To select/exclude only few elements that are needed
Product.find().select('title price -_id') --> select only title, price and exclude _id from the data
.populate('userId', 'name') --> second arg in the populate also provide the column names of the collection

we can add our own methods to the schema using following syntax

userSchema.methods.addToCart = function(){
}

to populate the cart.items.productId from the users collection
req.user.populate('cart.items.productId')


req.user.populate('cart.items.productId')
    .then(cartProducts => {
      const products = cartProducts.cart.items.map(item=>{
        return {quantity: item.quantity, product: item.productId}; --> here the product: item.productId stores only object ID
      });
--> but if we want to store the whole data in the product id
--> we can access the _doc hidden parameter to access all the parameters like product: {...item.productId._doc}
------------------------------------------------------------------------------------

cookies

req.isLoggedIn = true; // setting this before the response doesn't have any use
res.redirect('/'); // this si a brand new request, so the above info is already lost

we use cookie to store it in browser, so it will not effect other users
we set Headers in the response using the following syntax

res.setHeader('Set-Cookie', 'loggedIn=true') --> value will be the key value pair
console.log(req.get('Cookie')); --> we can retrieve the Cookies usign req.get('Cookie') --> multiple cookies as string
--> multiple cookies are splited by ; to retreive them seperately
--> better to not save sensitive info in the cookies as it is editable

res.setHeader('Set-Cookie', 'loggedIn=true; Max-Age:10')--> configure 10 sec expire time
Domain= or Secure (means HTTPS only) or httponly

cookies are strored on client side
sessions are stored in server-side

npm install --save express-session

initialize the session in app.js

app.use(session({
    secret:'secret-in-app',
    resave: false,
    saveUnintialized: false
}))

npm install --save connect-mongodb-session

const session = require('express-session');
const MongoDBStore = require('connect-mongodb-session')(session);
req.session.destroy(); --> to destroy the session

we stored req.session.userId = userId --> int eh niddleware function --> retreive the userId and get user 
then save to req.user = user for all requests

More on Sessions: https://www.quora.com/What-is-a-session-in-a-Web-Application

More on Cookies: https://developer.mozilla.org/en-US/docs/Web/HTTP/Cookies

Express-session Official Docs: https://github.com/expressjs/session
----------------------------------------------------------------------------------------

Authentication sign up/in --> session based authentication approach
to save passwords, we need to encrypt it first
so we use bcryptjs library
npm install --save bcryptjs

use bcrypt.hash(password, 12) --> second argument is how many times this hash function to be applied
--> this returns a promise, so we can use then()

we can add middleware file function to protect routes --> it goes from left to right (redirects to login form)
router.get('/add-product', isAuth, adminController.getAddProduct);

CSRF Attacks --> Cross-Site Request Forgery
manipulate/abuse sessions and and execute malicious code
make users access our application with the rendered views by our application, so the session is not available on any fake pages

npm install --save csurf --> pacakge used to create csrf tokens, that we can embbed into our forms or pages for every req that does something on backend
that changes the user state, and on the server side it checks whether the incoming request has the csrf token or not

After importing we can create 
const csrfProtection = csrf(); --> we can even configure inside csrf({ }), but default one works fine
--> csrfProtection --> is a middleware, we have to use it as one
app.use(csrfProtection); --> add it to our app

for any post request this pacakge will look for csrf token in the request body
so to add it to the request 
add it to the responseas follows
key is as our wish, value is calling csrfToken() from req.csrfToken()

csrfToken: req.csrfToken()

render the csrfToken value in ejs file as an hidden input value with a name="_csrf" because the package will look for this specific name
<input type="hidden" name="_csrf" value="<%=csrfToken%>"> 

(https://www.npmjs.com/search?q=express%20csrf). You may, for example, 
consider using this package (which has a different API than csurf()): https://www.npmjs.com/package/csrf-csrf.

Please understand that support in this course currently still focuses on the csurf API, as shown in the course videos and code snapshots.

providing user feedback on login for invalid or other cases,
we can send error msg using sessions --> after accessing it delete it so next calls don't pick it up
we have a pacakge for the same process

npm install --save connect-flash

postLogin
req.flash('error', 'Invalid email or password!'); --> add flash error key value pair to the request before redirecting to the login pages
getLogin
errorMessage: req.flash('error') --> in the response object, it is a list of errorMessages or empty array

Bcrypt Official Docs: https://github.com/dcodeIO/bcrypt.js
More on CSRF Attacks: https://www.acunetix.com/websitesecurity/csrf-attacks/
---------------------------------------------------------------------------------------------------------------

Using send grid to send mails from node js app

npm install --save nodemailer nodemailer-sendgrid-transport

const nodemailer = require('nodemailer');
const sendgridTransport = require('nodemailer-sendgrid-transport');

const transporter = nodemailer.createTransport(sendgridTransport({
    auth: {
        api_key: ''--> configure
    }
}));

we can use transporter to send the mails

    res.redirect('/login');
    return transporter.sendMail({
        to: email,
        from: 'shop@comp.com',
        subject: 'mail subject',
        html: '<h1>success</h1>'
    });

}).then(result=>{

});

const crypto = require('crypto');


logic to getNewPassword page load by taking token from params from request and check in the User DB if the valid token present

User.findOne({ resetToken: token, resetTokenExpiration: { $gt: Date.now() } }).then(user => {
    res.render('auth/newPassword', {
      path: '/new-password',
      pageTitle: 'New Password',
      // isAuthenticated: false,
      errorMessage: req.flash('error'),
      userId: user._id.toString()
    });
  }).catch(err => console.log(err));

filter on methods 

Product.findByIdAndDelete(productId) instead of this we can use Product.deleteOne({_id: productId, userId: req.user._id})
--------------------------------------------------------------------------------------------------------------------------------

Validation on forms

--> validations on client side
--> validations on server side
npm install --save express-validator

Express-Validator Docs: https://express-validator.github.io/docs/
Validator.js (which is used behind the scenes) Docs: https://github.com/chriso/validator.js

const {check} = require('express-validator/check'); //gives us a check function whihc returns a middleware
router.post('/signup', check('email').isEmail(), authController.postSignup); --> easy to intergrate as the middleware in router
router.post('/signup', check('email').isEmail().withMessage("Invalid email"), authController.postSignup); --> also add custom errorMessages

---> custom validators along with builtin ones, it is important to have th evalue custom(()=>{});
router.post('/signup', check('email').isEmail().withMessage("Invalid email")
    .custom((value, {req})=>{
        if(value=="test@test.com"){
            throw new Error("This email address is forbidden");
        }
        return true;
}), authController.postSignup);

---> second argument i can use the default error message, but individual messages overrides it if provided 
--> body is imported and used for password match as follows
body('confirmPassword', 'passwords are not same, please enter same passwords').custom((value, {req})=>{
        if(value !== req.body.password){
            throw new Error("Password have to match");
        }
        return true;
    })

const { validationResult } = require('express-validator/check'); --> in controllers to cehck all teh validation Errors being thrown in router

in the middleware in controllers i will extract the Errors
const errors = validationResult(req);
if(errors.isEmpty()) --> true if no errors

errors.array() --> returns the array of errors

if(!errors.isEmpty()) return res.status(422).render()--> common code for validation failed


adding Async validations for user exists in db for signup

inside the custom(value, {req})=>{
    return User.findOne({email: value})
        .then(user=>{
            if(user){
                // req.flash('error', 'Email already exisits');
                // return res.redirect('/signup');
                return Promise.reject('Email already exisits'); ---> using promise reject , 
                                                                    so if the return value is promise the pacakge waits for the promise to be
                                                                    fullfillment, if none returned/ no errors
            }
        })
}

normalizeEmail() (make email lower case and normalize it), trim() --> built-in sanitizer functions
--------------------------------------------------------------------------------------------------------

Errors handling
Tech/nw Errors --> MongoDB server is down, show error page on UI to user
Expected Errors --> File can't be read/db fails, INfrom usre, possibly retry 
Bug/Logical errors--> code errors, fix during dev

Error is thrown
    sync code --> use try-catch blocks
    Async code --> then-catch blocks
    Direct error handling or error handling middleware

No error is thrown
    validate values --> either throw error or directly handle error

Error page, error information, redirects

const error = new Error(err);
error.httpStatusCode = 500;
return next(error); --> tells node that error occurs and skip all middle wares and directly got to error handling middleware

in app.js

app.use((error, req, res, next)=>{
    res.redirect('/500'); --> instead of redirect, render so we don't go to infifnte loop
});

sync code --> throw new Error() --> goes to error middleware
but Async code --> we have to use next(new Error("error in async code")) in catch block -> to reach error middleware (we have throw new error() in then block)

MDN has a nice list: https://developer.mozilla.org/en-US/docs/Web/HTTP/Status

Or, as a short overview:

1×× Informational

100 Continue

101 Switching Protocols

102 Processing

2×× Success

200 OK

201 Created

202 Accepted

203 Non-authoritative Information

204 No Content

205 Reset Content

206 Partial Content

207 Multi-Status

208 Already Reported

226 IM Used

3×× Redirection

300 Multiple Choices

301 Moved Permanently

302 Found

303 See Other

304 Not Modified

305 Use Proxy

307 Temporary Redirect

308 Permanent Redirect

4×× Client Error

400 Bad Request

401 Unauthorized

402 Payment Required

403 Forbidden

404 Not Found

405 Method Not Allowed

406 Not Acceptable

407 Proxy Authentication Required

408 Request Timeout

409 Conflict

410 Gone

411 Length Required

412 Precondition Failed

413 Payload Too Large

414 Request-URI Too Long

415 Unsupported Media Type

416 Requested Range Not Satisfiable

417 Expectation Failed

418 I'm a teapot

421 Misdirected Request

422 Unprocessable Entity

423 Locked

424 Failed Dependency

426 Upgrade Required

428 Precondition Required

429 Too Many Requests

431 Request Header Fields Too Large

444 Connection Closed Without Response

451 Unavailable For Legal Reasons

499 Client Closed Request

5×× Server Error

500 Internal Server Error

501 Not Implemented

502 Bad Gateway

503 Service Unavailable

504 Gateway Timeout

505 HTTP Version Not Supported

506 Variant Also Negotiates

507 Insufficient Storage

508 Loop Detected

510 Not Extended

511 Network Authentication Required

599 Network Connect Timeout Error

Source: https://httpstatuses.com/

Error Handling in Express.js - Official Docs: https://expressjs.com/en/guide/error-handling.html
----------------------------------------------------------------------------------------------------------------

File upload --> but the urlencoded in app.js works only for text data

npm install --save multer --> parse incoming files

<form method="POST" enctype="multipart/form-data"> --> add enctype format
<input name="image" type"file">

app.use(multer({dest: 'images}).single('image')); --> in backend app.js use multer and single (single file) and name for the input 'image'
--> we can store the files in thedestination : images folder
--> we can have storage in the configuration instead of destination for extra features
app.use(multer({storage: fileStorage, fileFilter: fileFilter}).single('image'));
const fileStorage = multer.diskStorage({
    destination: (req, file, cb)=>{
        cb(null, 'images'); --> null says multer to okay to store it

    } 
    filename: (req, file, cb)=>{
        // cb(null, file.filename+'-'+file.originalname);
        cb(null, new Date().toISOString()+'-'+file.originalname);
    }
}); 

--> we can also add filter to the file type
const fileFilter = (req, fiel, cb)=>{
    if(file.mimetype === 'image/png' || file.mimetype === 'image/jpg'  || file.mimetype === 'image/jpeg'){
        cb(null, true); --> to store the file
    }
    else{
        cb(null, false); --> to not store the file
    }
}

req.file stores all information about the file with {fieldname: ..,  originalname:.., encoding:.., mimetype:.., buffer..., size:.. }

app.use('/images', express.static(path.join(__dirname, 'images'))); --> images under this folder also serves now - we also need to modify the path here of the req, 

--> but it checks the /admin/images folder whihc is not there
--> so we make changes in ejs to make the path absolute

<img src="/<%= product.imageUrl %>" alt="<%= product.title %>">

-->for small files sending the response as file is okay, but for large file we need to stream the data
exports.getInvoice = (req, res, next) => {
  const orderId = req.params.orderId;
  const invoiceName = 'invoice-'+orderId+'.pdf';
  const InvoicePath = path.join('data', 'invoices', invoiceName);
  console.log(InvoicePath);
  fs.readFile(InvoicePath, (err, data)=>{ //--> read file and send response by setting headers
    if(err){
      return next(err);
    }

    res.setHeader("Content-Type", "application/pdf");
    res.setHeader("Content-Disposition", 'inline; filename="'+invoiceName+'"'); --> inline opens the pdf in browser
    res.setHeader("Content-Disposition", 'attachment; filename="'+invoiceName+'"'); --> open the downloadable file to save in localhost
    res.send(data);
  })
}; 

--> Let's see how to stream the data

const file = fs.createReadStream(InvoicePath);

res.setHeader("Content-Type", "application/pdf");
res.setHeader("Content-Disposition", 'attachment; filename="'+invoiceName+'"');
file.pipe(res) --> pipe the stream into a writable stream

--> we can create the pdf on fly with real invoice data instead of using the static file  using PDFKit package

npm install --save pdfkit

const pdfDoc = new pdfkit(); --> 

res.setHeader("Content-Type", "application/pdf");
res.setHeader("Content-Disposition", 'attachment; filename="'+invoiceName+'"');

pdfDoc.pipe(fs.createWriteStream(InvoicePath)); --> create a writable stream

pdfDoc.fontSize(26).text("Invoice", {underLine: true});
pdfDoc.text("-----------------------------------------");

let totalPrice = 0;
order.products.forEach(product=>{
    totalPrice += product.quantity * product.product.price;
    pdfDoc.fontSize(14).text(product.product.title+" - "+product.quantity + ' x $'+ product.product.price);
})

pdfDoc.text('Total Price : $'+totalPrice);

pdfDoc.pipe(res);

pdfDoc.text('Hello World!'); --> add text to stream
pdfDoc.end(); --> end after adding the text


----> deleting a file
fs.unlink() --> it deletes the named file

Multer Official Docs: https://github.com/expressjs/multer
Streaming Files: https://medium.freecodecamp.org/node-js-streams-everything-you-need-to-know-c9141306be93
Generating PDFs with PDFKit: http://pdfkit.org/docs/getting_started.html
--------------------------------------------------------------------------------------------------------------------

pagination

<section class="pagination"> --> add page links in the ejs
    <a href="/?page=1">1</a>
    <a href="/?page=2">2</a>
</section>

now get the ?page=1 --> query parameter in the controller
const page = req.query.page;

 Product.find()
  .skip((page-1)*ITEMS_PER_PAGE) --> skip this many items
  .limit(ITEMS_PER_PAGE) --> limit the no. of items/records
    .then(products => {
        ....
    })

 Product.find()
    .countDocuments() --> to count the total nums of documents
    .then(numProducts => {
      totalItems = numProducts;
      console.log("totalItems : ", totalItems);
      return Product.find().skip((page - 1) * ITEMS_PER_PAGE) --> skip
        .limit(ITEMS_PER_PAGE) --> then limit--> then we can send currPage, previous page, next page, last page details in the response
    })
    .then(products => {
        ...
    })
-----------------------------------------------------------------------------------------------------------------------------------------

Async requests
--> in the app if we delete a product, it reloads whole page by giving removed product version list 
--> instead we can send the request and after successfull deletion we send a json object to frontend after whihc we just delete the dom element of that product

<button class="btn" type="button" onclick="deleteProduct(this)" >Delete</button> --> instead of form submit button
--> we use normal button with onclick = function with context function(this)
--> we have btn in the function btn.parentNode and get inputs for that Div

we can use delete route from JS script code 
--> delte request doesn't have body
fetch('/admin/product/'+productId, {
    method: 'DELETE',
    headers: {
        'csrf-token': csrf
    }
}).then(result=>{
    console.log(result);
}).catch(err=>{
    console.log(err);
})
---> for this request to be parsed (Json data), we need to add new parser for Json when we need it
--> after getting success json message response, we have to manipulate the DOM 

productElement.parentNode.removeChild(productElement); --> remove the child DOM after getting the parentNode (parent DOM)

More on the fetch API: https://developers.google.com/web/updates/2015/03/introduction-to-fetch
More on AJAX Requests: https://developer.mozilla.org/en-US/docs/Web/Guide/AJAX/Getting_Started






